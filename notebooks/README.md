# Notebooks

Exploratory analysis and experiment notebooks live here.

- Notebooks are for exploration; production code belongs in
  `src/micromagnetic_parameter_inversion/`.
- Use the project `.venv` as the kernel (see README "VS Code" section).
- Outputs with large data or checkpoints must not be committed; keep
  notebooks free of absolute machine-specific paths.