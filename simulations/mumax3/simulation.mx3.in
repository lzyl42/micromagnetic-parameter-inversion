// =============================================================================
// simulation.mx3.in —— 单脉冲自由衰减模板（结构骨架，未经真实 MuMax3 验证，
// 不可直接运行）。每个 pulse 渲染并执行一次：进程工作目录 = runs/<pulse_id>/，
// 渲染脚本固定名 simulation.mx3；MuMax3 默认原生输出目录 simulation.out/
// （table.txt、m_t0.ovf、m_tfinal.ovf、log.txt、references.bib）；run.log 与
// trajectory.csv 由 pipeline 写在 pulse 目录根。
// 与 equilibrium.mx3.in 渲染自同一 config（同一网格/材料/几何，保证一致）。
// =============================================================================

// 1. 固定模型、网格与材料（与 equilibrium.mx3.in 区段 1-2 完全一致，
//    由同一 config 渲染；TODO: 确切命令待核实后固定）

// 2. 加载共享平衡态（固定相对路径，相对 runs/<pulse_id>/）
// TODO: LoadFile("../../equilibrium/equilibrium.out/equilibrium.ovf")
//       的确切命令名与行为待核实；只加载，不得重新弛豫。

// 3. 实验真实 alpha（平衡态已冻结在 OVF 中，此处不得再用辅助阻尼）
// TODO: alpha = {{ALPHA}} 的设置命令待核实。

// 4. 矩形脉冲与精确关场
// TODO: t=0 起施加 B_ext（{{B_EXT_T}} 与方向 {{DIRECTION}} 逐分量相乘，
//       单位 T），持续 {{PULSE_DURATION_S}} s 后精确置 0；Run() 步进与
//       关场写法待核实后固定。

// 5. 关场后状态即训练时间零点（sample_index=0、t_s=0）：
//    保存 m_t0.ovf（QC）-> 固定间隔（{{SAMPLE_INTERVAL_S}} s）自由衰减，
//    记录 {{SAMPLE_COUNT}} 个样本 -> 保存 m_tfinal.ovf（QC）；三者均落在
//    simulation.out/ 内，table.txt 为原生采样表。
// TODO: TableAutoSave 与显式 Run(dt)+TableSave 二选一，待真实单 cell
//       对比后只固定一种；不引入 YAML 开关。
